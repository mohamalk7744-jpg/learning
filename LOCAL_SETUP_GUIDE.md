# 🚀 دليل تشغيل تطبيق التعلم عن بعد محلياً

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

### 1. **Node.js و npm/pnpm**
```bash
# تحقق من الإصدار
node --version  # يجب أن يكون v18 أو أعلى
npm --version   # أو pnpm --version
```

### 2. **Expo CLI** (للتطبيقات الجوالة)
```bash
npm install -g expo-cli
```

### 3. **Expo Go App** (على هاتفك)
- قم بتحميل تطبيق **Expo Go** من:
  - 📱 **iOS**: App Store
  - 🤖 **Android**: Google Play Store

---

## 🔧 خطوات التثبيت والتشغيل

### الخطوة 1: استنساخ/تحميل المشروع
```bash
cd ~/e_learning_mobile_app
```

### الخطوة 2: تثبيت المكتبات
```bash
pnpm install
# أو إذا كنت تستخدم npm
npm install
```

### الخطوة 3: بدء خادم التطوير
```bash
pnpm start
# أو
npm start
```

ستظهر رسالة مثل:
```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code below with Expo Go (Android) or the Camera app (iOS)
```

### الخطوة 4: فتح التطبيق على هاتفك

#### **على iOS:**
1. افتح تطبيق **الكاميرا** (Camera)
2. وجّه الكاميرا نحو **QR Code** الظاهر في الطرفية
3. اضغط على الإشعار الذي يظهر
4. سيفتح التطبيق في Expo Go

#### **على Android:**
1. افتح تطبيق **Expo Go**
2. اضغط على **Scan QR Code**
3. وجّه الكاميرا نحو **QR Code**
4. سيفتح التطبيق مباشرة

---

## 📱 اختبار التطبيق

### الشاشات المتاحة:

#### 1. **الصفحة الرئيسية (Home)**
- ✅ عرض معلومات الطالب
- ✅ عرض إحصائيات اليوم
- ✅ روابط سريعة للأقسام الأخرى
- ✅ زر تسجيل الخروج

#### 2. **الخطة الدراسية (Schedule)**
- 📚 عرض الدروس اليومية
- ✏️ عرض الاختبارات اليومية
- 📊 عرض التقدم الدراسي
- ✅ أزرار "ابدأ" و "اختبر" تعمل بنجاح

#### 3. **البوت الذكي (Chat)**
- 💬 واجهة دردشة
- 🤖 رسائل من البوت
- 📝 إرسال الأسئلة
- ⚠️ ملاحظة: البوت الذكي يحتاج دمج OpenAI API

#### 4. **الحسومات (Discounts)**
- 🎉 عرض العروض والحسومات
- 💰 عرض نسبة الخصم
- ✅ زر "استخدم الآن" يعمل بنجاح

#### 5. **الاختبارات (Exams)**
- 📋 عرض الاختبارات الفصلية والشهرية
- 📊 عرض النتائج والدرجات
- 📤 رفع صور الحلول
- ✅ جميع الأزرار تعمل بنجاح

---

## 🔄 إعادة تحميل التطبيق

أثناء التطوير، قد تحتاج لإعادة تحميل التطبيق:

### على الهاتف:
- **iOS**: اهز الهاتف (Shake) أو اضغط Cmd+D
- **Android**: اضغط Cmd+M أو اهز الهاتف

### في الطرفية:
- اضغط `r` لإعادة تحميل
- اضغط `w` لفتح في المتصفح
- اضغط `q` للخروج

---

## 🌐 تشغيل على المتصفح

لتشغيل التطبيق على متصفح الويب:

```bash
pnpm start
# ثم اضغط 'w' في الطرفية
```

سيفتح التطبيق على `http://localhost:19006`

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يظهر QR Code
**الحل:**
```bash
# أعد تشغيل الخادم
pnpm start

# أو امسح الذاكرة المؤقتة
pnpm start --clear
```

### المشكلة: الهاتف لا يتصل بالخادم
**الحل:**
1. تأكد من أن الهاتف والكمبيوتر على نفس شبكة WiFi
2. تحقق من عنوان IP في الطرفية
3. جرّب إعادة تشغيل الخادم

### المشكلة: أخطاء TypeScript
**الحل:**
```bash
# امسح node_modules وأعد التثبيت
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

---

## 📦 البناء للإنتاج

### بناء APK لـ Android:
```bash
eas build --platform android
```

### بناء IPA لـ iOS:
```bash
eas build --platform ios
```

---

## 🎯 الميزات المتاحة الآن

✅ **مكتملة:**
- تسجيل الدخول والخروج
- الانتقال بين الشاشات
- عرض الدروس والاختبارات
- عرض الحسومات
- رفع صور الحلول

⚠️ **قيد التطوير:**
- دمج البوت الذكي مع OpenAI
- نظام الإشعارات الفعلي
- قاعدة البيانات الحقيقية

---

## 📚 موارد إضافية

- [Expo Documentation](https://docs.expo.dev/)
- [React Native Documentation](https://reactnative.dev/)
- [Expo Router Guide](https://expo.github.io/router/)

---

## ❓ أسئلة شائعة

**س: هل يعمل على iOS و Android؟**
ج: نعم! التطبيق يعمل على كلا النظامين بنفس الكود.

**س: هل أحتاج لـ Xcode أو Android Studio؟**
ج: لا، Expo Go يوفر بيئة تطوير كاملة بدونهما.

**س: كيف أشارك التطبيق مع الآخرين؟**
ج: أرسل لهم رابط QR Code أو رابط Expo الظاهر في الطرفية.

---

**آخر تحديث: 24 ديسمبر 2025**
